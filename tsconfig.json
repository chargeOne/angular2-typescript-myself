
{
  "compilerOptions": {
    "target": "es5",
    "module": "system",//'commonjs'，'amd'，'system'，或 'umd'。只有'amd'和'system'能和--outFile一起使用。
    "moduleResolution": "node",
    //"outFile": "dist.js",//将输出文件合并为一个文件。合并的顺序是根据传入编译器的文件顺序和///<reference``>和import的文件顺序决定的。查看输出文件顺序文件了解详情。
    "outDir": "./dist/app/",//输出路径
    "sourceMap":true,//如果为false就只输出.js
    "emitDecoratorMetadata": true,
    "experimentalDecorators": true,
    "removeComments": false,//编译 js 的时候，删除掉注释
    "noImplicitAny": false
  },
  "exclude": [
    "node_modules",
    "typings/main",
    "typings/main.d.ts"
  ]
}